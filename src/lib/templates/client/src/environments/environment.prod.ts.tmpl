// Add authorized roles by their name here. All of them will have access to the UI
export const environment = {
  production: true,
  apiUrl: 'http://localhost:4000/api',
  authorizedRole: ['Admin']
};
